; Booking attachments are limited to 5 MB each by Laravel validation.
; Keep the request limit larger so several attachments can be uploaded together.
upload_max_filesize=5M
post_max_size=128M
max_file_uploads=50
